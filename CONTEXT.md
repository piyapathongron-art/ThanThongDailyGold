# CONTEXT — DailyGold (ห้างทองธารทอง)

ป้ายราคาทองดิจิทัลสำหรับหน้าร้าน ประกอบด้วยจอทีวีในร้านและคอมพิวเตอร์ที่พนักงานใช้แก้ราคา

## Ubiquitous language

### Display Board (จอ)
หน้า `/` ที่ฉายบนทีวีในร้าน อ่านอย่างเดียว ไม่มีใครแตะ เปิดค้างไว้เป็นสัปดาห์
ทุกอย่างที่ตัดสินใจเรื่องความทนทาน (polling แทน websocket) มีจอนี้เป็นเหตุผล

### Dashboard (หน้าตั้งค่า)
หน้า `/edit` ที่พนักงานเปิดบนคอมในร้านเพื่อแก้ราคาและอัปโหลดโปรโมชั่น
**ไม่ใช่** หน้าเดียวกับ Display Board และไม่ได้อยู่บนเครื่องเดียวกัน

### Gold Bar (ทองคำแท่ง 96.5%) / Gold Ornament (ทองรูปพรรณ 96.5%)
สินค้าสองชนิดที่แสดงราคาแยกกัน แต่ละชนิดมี **Mode** ของตัวเอง

### Buy (ซื้อเข้า) / Sell (ขายออก)
ทิศทางราคาจากมุมของร้าน — `buy` คือราคาที่ร้านรับซื้อจากลูกค้า `sell` คือราคาที่ร้านขายให้ลูกค้า
ระวังสลับ: ในโค้ด `price.gold_bar.buy` = ราคาที่ร้าน**รับซื้อ**

### Mode (`api` | `manual`)
ที่มาของราคา **มีเฉพาะ Gold Bar เท่านั้น** `api` = ตามราคาตลาด `manual` = พนักงานกรอกเอง
เมื่อ **Price Source** ล่ม จะถูกบังคับลงเป็น `manual` อัตโนมัติ

**Gold Ornament ไม่มี Mode** — เป็นราคาที่พนักงานกรอกเองเสมอ ดู [ADR 0002](docs/adr/0002-gold-price-source.md)

### Price Source
`thaigold.info/RealTimeDataV2/gtdata_.txt` — feed ที่เผยแพร่ให้เครื่องอ่าน
แถวชื่อ `สมาคมฯ` คือราคาทองคำแท่งของสมาคม `bid` = ซื้อเข้า `ask` = ขายออก

**แหล่งเดิมสองชั้นตายทั้งคู่ อย่าพยายามกลับไปใช้:**
- `classic.goldtraders.or.th` — Cloudflare ตอบ **403** ให้ IP ดาต้าเซ็นเตอร์ ทดสอบแล้วทั้ง iad1 และ sin1
- `api.chnwt.dev` — คือ [max180643/thai-gold-api](https://github.com/max180643/thai-gold-api)
  ซึ่ง scrape หน้าเดียวกันด้วย cheerio โดน Cloudflare ส่งหน้า challenge กลับมาพร้อม
  status **200** โค้ดเลยผ่านด่านตรวจ status แต่หา selector ไม่เจอ → คืน `success` พร้อมราคาว่าง
  **เปิด repo นั้นเองไม่ช่วย** ตัวบล็อกคือ IP ไม่ใช่โค้ด

Feed นี้ไม่มีเวลาประกาศราคาของแถว `สมาคมฯ` มีแต่นาฬิกา refresh ของตัว feed เอง
จอจึงไม่แสดงเวลาอัปเดต ดีกว่าแสดงเวลาที่หมายถึงอย่างอื่น

### Display Settings
ชุด state ที่ Display Board กับ Dashboard ต้องเห็นตรงกัน: Mode ของทองคำแท่ง, ราคาที่กรอกเอง,
Promo Slides, และ `poll_seconds`

เก็บเป็น **แถวเดียว** ใน Postgres บน Supabase (โปรเจกต์ `bihgcdceovfettoxmgme`)
ไม่ใช่ localStorage อีกต่อไป — localStorage ผูกกับเบราว์เซอร์ ไม่ใช่ URL คอมกับทีวีจึงไม่มีวัน
ตรงกันได้เลยไม่ว่าเปิดลิงก์เดียวกันแค่ไหน ดู [ADR 0001](docs/adr/0001-server-side-display-settings.md)

Display Board **poll** แถวนี้ทุก `poll_seconds` วินาที การเขียนต้องมี `EDIT_PASSWORD`

### Promo Slide
รูปโปรโมชั่นหนึ่งใบที่หมุนแสดงบน Display Board มีได้ 0–5 ใบ

**จำนวนสไลด์ = จำนวนรูปที่อัปจริง ไม่ใช่ค่าที่ตั้งแยก** อัป 2 รูปก็วน 2 รูป
การมี "จำนวนช่อง" เป็นค่าตั้งต่างหากทำให้เกิดช่องว่างที่ไม่มีรูป แล้วจอจะฉายกล่องเปล่า
ให้ลูกค้าดู — เป็นอาการที่ระบบเดิมเป็นอยู่จริงเพราะ hardcode ไว้ 3 ช่องเสมอ

ไม่มีรูปเลย → ฉาย placeholder ใบเดียว

### Rotation (การหมุนจอ)
มุมหมุน 0/90/180/270 ตามการติดตั้งทีวีแต่ละเครื่อง
**เป็น per-device โดยเจตนา** ไม่อยู่ใน Display Settings — คอมกับทีวีติดตั้งคนละแนว
ถ้า sync จะทำให้จอคอมหมุนตามทีวี

## Constraints

- ทุกหน้าเป็น `"use client"` ไม่มี server component / server action
- ต้องมี hydration guard ทุกหน้าที่อ่าน store (กัน SSR/localStorage mismatch)
- React Compiler เปิดอยู่ใน `next.config.ts`
